--- a/src/client/client-common/utils/network_utils/network_utils_linux.cpp
+++ b/src/client/client-common/utils/network_utils/network_utils_linux.cpp
@@ -423,28 +423,8 @@
 
 static QString getNetworkForInterface(const QString &ifname)
 {
-#ifdef CLI_ONLY
-    // When using CLI only, the network is likely not managed by nmcli, even if network-manager is even installed.
-    // Instead, we use iwgetid to get the SSID if it is Wi-Fi.  Otherwise, the name is just the name of the interface.
-    if (!checkWirelessByIfName(ifname)) {
-        return ifname;
-    }
-
-    QString command = QString("iw dev ") + ifname + QString(" link | grep SSID | cut -d ' ' -f 2-");
-    FILE *file = popen(command.toStdString().c_str(), "r");
-    if (file) {
-        char szLine[1024];
-        char *ret = fgets(szLine, sizeof(szLine), file);
-        pclose(file);
-        if (ret != NULL) {
-            return QString(szLine).trimmed();
-        } else {
-            return "";
-        }
-    }
-#else
     QString strReply;
-    FILE *file = popen("nmcli -t -f NAME,DEVICE c show", "r");
+    FILE *file = popen("nmcli -t -f NAME,DEVICE c show 2>/dev/null", "r");
     if (file) {
         char szLine[4096];
         while(fgets(szLine, sizeof(szLine), file) != 0) {
@@ -456,13 +436,25 @@
     const QStringList lines = strReply.split('\n', Qt::SkipEmptyParts);
     for (auto &it : lines) {
         const QStringList pars = it.split(':', Qt::SkipEmptyParts);
-        if (pars.size() == 2) {
-            if (pars[1] == ifname) {
-                return pars[0];
-            }
+        if (pars.size() == 2 && pars[1] == ifname) {
+            return pars[0];
+        }
+    }
+
+    // windscribe-nixos: fallback when NetworkManager is unavailable, SSID via iw for Wi-Fi, ifname for wired.
+    if (!checkWirelessByIfName(ifname)) {
+        return ifname;
+    }
+    QString command = QString("iw dev ") + ifname + QString(" link | grep SSID | cut -d ' ' -f 2-");
+    FILE *iwfile = popen(command.toStdString().c_str(), "r");
+    if (iwfile) {
+        char szLine[1024];
+        char *ret = fgets(szLine, sizeof(szLine), iwfile);
+        pclose(iwfile);
+        if (ret != NULL) {
+            return QString(szLine).trimmed();
         }
     }
-#endif
     return QString();
 }
 
--- a/src/helper/linux/utils.cpp
+++ b/src/helper/linux/utils.cpp
@@ -160,7 +160,6 @@
     return false;
 }
 
-#ifdef CLI_ONLY
 static std::vector<std::string> getInterfaceNames()
 {
     std::string output;
@@ -231,35 +230,32 @@
     output.erase(output.find_last_not_of(" \n\r\t") + 1);
     return output;
 }
-#endif
 
 bool resetMacAddresses(const std::string &ignoreNetwork)
 {
-#ifdef CLI_ONLY
-    std::vector<std::string> interfaces = getInterfaceNames();
-
-    for (const auto interface : interfaces) {
-        // skip ignored network
-        if (interface == ignoreNetwork) {
-            continue;
-        }
-
-        std::string hwAddr = getHwMac(interface);
-        std::string curAddr = getCurrentMac(interface);
-        if (hwAddr != curAddr && hwAddr != "not set") {
-            spdlog::info("Resetting MAC spoofing on {} (hw: {}, cur: {})", interface, hwAddr, curAddr);
-            // Must bring down interface to change the MAC address
-            Utils::executeCommand("ip", {"link", "set", "dev", interface, "down"});
-            Utils::executeCommand("ip", {"link", "set", "dev", interface, "address", hwAddr});
-            Utils::executeCommand("ip", {"link", "set", "dev", interface, "up"});
+    std::string output;
+    int ret = Utils::executeCommand("nmcli", {"--fields", "state,name", "connection", "show"}, &output);
+    if (ret != 0 || output.empty()) {
+        // windscribe-nixos: fallback when NetworkManager is unavailable, reset via iproute2.
+        std::vector<std::string> interfaces = getInterfaceNames();
+        for (const auto interface : interfaces) {
+            if (interface == ignoreNetwork) {
+                continue;
+            }
+            std::string hwAddr = getHwMac(interface);
+            std::string curAddr = getCurrentMac(interface);
+            if (hwAddr != curAddr && hwAddr != "not set") {
+                spdlog::info("Resetting MAC spoofing on {} (hw: {}, cur: {})", interface, hwAddr, curAddr);
+                Utils::executeCommand("ip", {"link", "set", "dev", interface, "down"});
+                Utils::executeCommand("ip", {"link", "set", "dev", interface, "address", hwAddr});
+                Utils::executeCommand("ip", {"link", "set", "dev", interface, "up"});
+            }
         }
+        return true;
     }
-#else
-    std::string output;
+
     std::string line;
     bool firstline = true;
-    Utils::executeCommand("nmcli", {"--fields", "state,name", "connection", "show"}, &output);
-
     std::stringstream is(output);
     while (std::getline(is, line)) {
         // skip labels
@@ -297,7 +293,6 @@
             Utils::executeCommand("nmcli", {"connection", "up", "id", name.c_str()});
         }
     }
-#endif
     return true;
 }
 
--- a/src/helper/linux/process_command.cpp
+++ b/src/helper/linux/process_command.cpp
@@ -456,25 +456,26 @@
     // reset addresses on other networks
     Utils::resetMacAddresses(network);
 
-#ifdef CLI_ONLY
-    // Must bring interface down to change the MAC address
-    Utils::executeCommand("ip", {"link", "set", "dev", interface, "down"});
-    Utils::executeCommand("ip", {"link", "set", "dev", interface, "address", mac});
-    Utils::executeCommand("ip", {"link", "set", "dev", interface, "up"});
-#else
     std::string out;
+    int ret;
     // Pass the connection name with an explicit "id" selector so nmcli always treats it as a name,
     // never as one of its own selector keywords (id/uuid/path) that would otherwise consume the
     // following token. isValidNetworkManagerConnectionName already blocks a leading '-'; this closes
     // the same argv-semantics gap for a connection literally named "id"/"uuid"/"path".
     if (isWifi) {
-        Utils::executeCommand("nmcli", {"connection", "modify", "id", network, "wifi.cloned-mac-address", mac}, &out);
+        ret = Utils::executeCommand("nmcli", {"connection", "modify", "id", network, "wifi.cloned-mac-address", mac}, &out);
     } else {
-        Utils::executeCommand("nmcli", {"connection", "modify", "id", network, "ethernet.cloned-mac-address", mac}, &out);
+        ret = Utils::executeCommand("nmcli", {"connection", "modify", "id", network, "ethernet.cloned-mac-address", mac}, &out);
+    }
+    if (ret == 0) {
+        // restart the connection
+        Utils::executeCommand("nmcli", {"connection", "up", "id", network});
+    } else {
+        // windscribe-nixos: fallback when NetworkManager is unavailable, set MAC via iproute2.
+        Utils::executeCommand("ip", {"link", "set", "dev", interface, "down"});
+        Utils::executeCommand("ip", {"link", "set", "dev", interface, "address", mac});
+        Utils::executeCommand("ip", {"link", "set", "dev", interface, "up"});
     }
-    // restart the connection
-    Utils::executeCommand("nmcli", {"connection", "up", "id", network});
-#endif
     return serializeResult(true);
 }
 
